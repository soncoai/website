{
  "name": "sonnco-website",
  "version": "1.0.0",
  "description": "Sonnco marketing landing page — static site built with Tailwind CSS.",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./css/site.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./css/site.css --watch",
    "serve": "python3 -m http.server 8000",
    "dist": "rm -rf dist && mkdir dist && cp -R index.html 404.html _headers css js fonts images dist/",
    "deploy": "npm run build && npm run dist && wrangler deploy"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "alpinejs": "^3.15.12",
    "tailwindcss": "^4.3.3",
    "wrangler": "^4.130.0"
  }
}
